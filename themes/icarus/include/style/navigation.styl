.columns {
  display: flex;
  flex-wrap: wrap; /* 允许列换行 */
  gap: 20px; /* 定义列之间和行之间的空白 */
  justify-content: center; /* 居中显示列 */
}

.column {
  flex: 1 1 200px; /* 每列尽可能占据200px宽度，但允许灵活伸缩 */
  /* 添加更多样式（如padding, border等）根据需要 */
}